name: appraisalpro-web
region: nyc
services:
  - name: appraisalpro-web
    github:
      branch: master
      repo: logan602/AppraisalPro-Web
      deploy_on_push: true
    source_dir: /
    build_command: "npx prisma generate && npm run build"
    run_command: "npx prisma db push --accept-data-loss && npm start"
    environment_slug: node-js
    instance_count: 1
    instance_size_slug: basic-xxs
    http_port: 3000
    envs:
      - key: DATABASE_URL
        scope: RUN_AND_BUILD_TIME
        value: ${appraisalpro-db.DATABASE_URL}
      - key: JWT_SECRET
        scope: RUN_TIME
        value: "generate_a_secure_random_string_here_before_prod"
databases:
  - name: appraisalpro-db
    engine: PG
    version: "15"
    production: true
    cluster_name: appraisalpro-db-cluster
